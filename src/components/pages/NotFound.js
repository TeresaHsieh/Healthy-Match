import React from "react";

// App Components
import Header from "../common/Header";

const NotFound = () => (
    <div>
        <Header />
        <h2> NotFound </h2>
    </div>
);

export default NotFound;